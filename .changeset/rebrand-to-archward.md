---
'@archward/ddd-eslint-plugin': patch
---

Rebrand: this package is now published as `@archward/ddd-eslint-plugin` (formerly `@ddd-arch/eslint-plugin`, which is deprecated and points here). No API or behaviour change — only the package name, scope, and repository URLs.
