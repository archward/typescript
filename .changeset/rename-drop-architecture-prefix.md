---
'@archward/eslint-plugin': minor
---

Rename from `@archward/ddd-eslint-plugin`, dropping the architecture prefix — one plugin per language with architectures as composed config (ADR-0014). The old package is deprecated and points here.
